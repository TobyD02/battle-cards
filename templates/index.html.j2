<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/static/card.css">
    <title>{{ card_model.name }} - Holo Rare</title>

</head>

<body>

<div style="display: grid;grid-template-columns: auto auto auto; gap: 10px;">


    <div class="card card--holo">
        <img class="card__image" src="{{ card_model.image_url }}" alt="{{ card_model.name }}">

        <div class="card__holo-overlay"></div>

        <div class="card__info">
            <div class="card__info-name">{{ card_model.name }}</div>

            <div class="card__info-stats">
                <div><strong>STR:</strong> {{ card_model.strength }}</div>
                <div><strong>DEX:</strong> {{ card_model.dexterity }}</div>
                <div><strong>WIS:</strong> {{ card_model.wisdom}}</div>
                <div><strong>INT:</strong> {{ card_model.intelligence}}</div>
                <div><strong>CHA:</strong> {{ card_model.charisma}}</div>
            </div>

            <div class="card__info-description">
                {{ card_model.description }}
            </div>
        </div>
    </div>

    <div class="card card--regular">
        <img class="card__image" src="{{ card_model.image_url }}" alt="{{ card_model.name }}">

        <div class="card__holo-overlay"></div> <div class="card__info">
        <div class="card__info">
            <div class="card__info-name">{{ card_model.name }}</div>

            <div class="card__info-stats">
                <div><strong>STR:</strong> {{ card_model.strength }}</div>
                <div><strong>DEX:</strong> {{ card_model.dexterity }}</div>
                <div><strong>WIS:</strong> {{ card_model.wisdom}}</div>
                <div><strong>INT:</strong> {{ card_model.intelligence}}</div>
                <div><strong>CHA:</strong> {{ card_model.charisma}}</div>
            </div>

            <div class="card__info-description">
                {{ card_model.description }}
            </div>
        </div>
    </div>
</div>
</div>

</body>
</html>
